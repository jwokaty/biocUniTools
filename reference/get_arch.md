# Get the arch

Get the arch

## Usage

``` r
get_arch(binaries_arch, jobs_arch)
```

## Arguments

- binaries_arch:

  \_binaries_arch

- jobs_arch:

  \_jobs_arch

## Value

character

## Examples

``` r
get_arch(NA, "arm64")
#> Error in get_arch(NA, "arm64"): could not find function "get_arch"
```
